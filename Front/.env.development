# Integração com API real (Docker ou local). Mocks desativados; requisições vão para VITE_API_URL.
VITE_API_URL=http://localhost:5002/api/v1
# Use VITE_ENABLE_MOCKS=true apenas para desenvolver sem backend (MSW).
# VITE_ENABLE_MOCKS=
