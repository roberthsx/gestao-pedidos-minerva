# Copie para .env.local e preencha os valores.
# Nunca commite .env.local ou arquivos com credenciais reais.
#
# API V1 (base URL; ex: http://localhost:5002/api/v1 para integração com Docker)
VITE_API_URL=http://localhost:5002/api/v1
# Mocks (MSW): true = intercepta requisições; false/omitido = usa API real em VITE_API_URL
VITE_ENABLE_MOCKS=
#
# Infraestrutura (placeholders — em produção use Secrets Manager / variáveis do pipeline)
# Nunca salve chaves AWS ou encryption no repositório.
VITE_AWS_REGION=
VITE_AWS_ACCESS_KEY_ID=
VITE_ENCRYPTION_KEY=
