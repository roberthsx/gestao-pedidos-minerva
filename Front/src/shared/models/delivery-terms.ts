/** Interface baseada na modelagem: entidade DeliveryTerms (gerado por worker após criação). */
export interface DeliveryTerms {
  id: string
  orderId: string
  estimatedDelivery?: string
  terms?: string
}
