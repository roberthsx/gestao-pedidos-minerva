# =============================================================================
# Minerva.GestaoPedidos - Variáveis de ambiente para CI/CD e execução local
# =============================================================================
# Copie para .env e preencha os valores. Nunca commite o arquivo .env.
# Adicione .env ao .gitignore. No Docker/Kubernetes, defina no pipeline/deployment.
# =============================================================================

# -----------------------------------------------------------------------------
# Ambiente ASP.NET
# -----------------------------------------------------------------------------
ASPNETCORE_ENVIRONMENT=Development
ASPNETCORE_URLS=http://0.0.0.0:8080

# -----------------------------------------------------------------------------
# Connection Strings (substituem appsettings quando definidas)
# Formato: ConnectionStrings__<Nome>=<valor>
# -----------------------------------------------------------------------------
# PostgreSQL (Write)
ConnectionStrings__Postgres=Host=postgres;Port=5432;Database=minerva_db;Username=admin;Password=admin_password;Pooling=true;MinPoolSize=5;MaxPoolSize=100;ConnectionLifetime=0;

# MongoDB (Read)
ConnectionStrings__Mongo=mongodb://admin:admin_password@mongodb:27017/minerva_db?authSource=admin&minPoolSize=10&maxPoolSize=100

# Redis (Cache JWT e outros) - chave usada: RedisConnection ou Redis
ConnectionStrings__RedisConnection=redis:6379,password=admin_password
ConnectionStrings__Redis=redis:6379,password=admin_password

# -----------------------------------------------------------------------------
# Kafka
# -----------------------------------------------------------------------------
Kafka__BootstrapServers=kafka:9092

# -----------------------------------------------------------------------------
# JWT e Autenticação (produção: use JWT_SECRET forte e nunca commite)
# -----------------------------------------------------------------------------
Jwt__Secret=MinervaFoods_Super_Secret_Key_2026_TechLead
Jwt__Issuer=Minerva.GestaoPedidos
Jwt__Audience=Minerva.Frontend
Jwt__ExpirationMinutes=60

# Variável de ambiente direta para o segredo (prioridade sobre appsettings)
# JWT_SECRET=MinervaFoods_Super_Secret_Key_2026_TechLead

# -----------------------------------------------------------------------------
# Database (opcional: UseInMemory=true para testes)
# -----------------------------------------------------------------------------
# Database__UseInMemory=false
